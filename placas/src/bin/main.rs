///fn main() {
///let inital = State {
///board: [1, 2, 3, 0, 4, 5, 6, 7, 8],
///blank_index: 3,
///};
///let desire = State {
///board: [0, 1, 2, 3, 4, 5, 6, 7, 8],
///blank_index: 0,
///};

///let node = bfs(inital, desire).unwrap();
/////let node = dfs(inital, desire).unwrap();
///println!("{:?}", node.state.board);
///print_path(&node.father.unwrap());
/////println!("{:?}", r);
///}
fn main() {}
